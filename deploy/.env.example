# 数据库配置
POSTGRES_DB=multiprotgather
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
DATABASE_URL=postgresql://postgres:postgres123@db:5432/multiprotgather

# Redis配置
REDIS_URL=redis://redis:6379/0

# Django配置
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:80,https://your-domain.com

# 邮件配置（可选）
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# 日志级别
LOG_LEVEL=INFO

# 文件上传配置
MAX_UPLOAD_SIZE=10485760  # 10MB

# 任务执行配置
TASK_TIMEOUT=300  # 5分钟
MAX_CONCURRENT_TASKS=10

# 监控配置
FLOWER_BASIC_AUTH=admin:password123

# Celery配置
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/1

# XXL-Job配置
XXL_JOB_ADMIN_URL=http://localhost:8080/xxl-job-admin
XXL_JOB_ACCESS_TOKEN=
XXL_JOB_EXECUTOR_APP_NAME=multiprotgather-executor
XXL_JOB_EXECUTOR_PORT=9999

# 邮件配置
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# 钉钉配置
DINGTALK_WEBHOOK_URL=
DINGTALK_SECRET=

# 登录配置
ENABLE_LOGIN=True
DEFAULT_USERNAME=admin
DEFAULT_PASSWORD=admin123

# 其他配置
ENCRYPTION_KEY=your-encryption-key-here